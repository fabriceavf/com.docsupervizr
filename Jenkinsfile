pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'docker ps'
                sh 'ls'
            }
        }
        stage('Test') {
            steps {
                sh 'ls'
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls'
            }
        }
    }
}